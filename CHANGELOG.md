# 版本记录

这里记录每个正式发布版本的新增、修改和修复。日常开发提交不会自动形成新版本；只有明确决定发布时才更新版本号和创建 Git tag。

## 0.1.2 — 2026-09-16

### 新增

- 增加 Windows 7 Aero 风格的专用程序图标。
- 提供包含 16、20、24、32、40、48、64、128 和 256 px 图像的多尺寸 ICO。

### 修改

- 编译后的 EXE 和直接运行的 AHK 脚本均使用专用图标，不再显示 AutoHotkey 默认图标。
- 针对托盘尺寸收紧图形边界，并对小尺寸帧做适量锐化。

## 0.1.1 — 2026-09-16

### 新增

- 实现基于 AutoHotkey v2、WinEvent、TSF 和 IMM32 的按窗口输入法记忆。
- 支持 English US 与微信输入法，以及微信输入法内部中文/英文状态。
- 支持默认输入法、自动学习、持久化、强制规则和托盘菜单。
- 提供独立 EXE、模块化源码、自检和只观察烟雾测试。
- 将规范源码放入安装根目录的 `src` 子目录，构建产物直接安装到上一级目录。

### 修改

- 已经符合目标 profile 和 IME 内部状态时，跳过整个恢复流程。
- 仅部分状态不同时，只写入实际发生变化的字段。
- 状态文件增加原子写入、备份和损坏回退。

### 修复

- 修复 AutoHotkey 不区分大小写导致局部变量遮蔽 `Buffer()` 类的问题。
- 未处理异常改为写入日志后退出，避免连续弹出错误窗口。
