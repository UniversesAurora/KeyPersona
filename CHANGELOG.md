# Changelog

## 1.1.0 — 2026-09-18

### 新增

- 托盘菜单增加“自动记忆输入法”开关。关闭后不读取或写入窗口记忆，只在进入窗口时执行匹配的用户规则。
- 托盘菜单增加“清除所有自动记忆…”，可同时清空会话记忆、`state.ini` 和 `state.ini.bak`，保留用户规则和全局默认设置。

### 修改

- 用户规则改为窗口没有自动记忆时的默认输入法，不再持续锁定输入法。
- 自动记忆开启时，优先使用会话记忆和持久记忆，再使用用户规则和全局默认。
- “设置当前窗口为”改为“当前窗口默认输入法”。设置新默认值时会清除该窗口的旧自动记忆。

### 修复

- 修复 Windows Terminal 等命中用户规则的窗口无法手动切换到其他输入法的问题。
- 清除全部记忆时同步刷新状态备份，避免旧 `state.ini.bak` 恢复已经清除的记录。

## 1.0.0 — 2026-09-17

### 新增

- 项目更名为 KeyPersona。升级时保留原有配置、窗口记忆和日志，并迁移旧自启动入口。
- 自动发现新安装的输入法；中文 TIP 会加入中文和英文两个可选状态，其他布局加入一个状态。
- 托盘菜单可以设置全局默认输入法、当前窗口用户规则和开机自启动。
- 菜单显示当前输入法、状态来源和窗口短 ID。
- 增加“中文模式下反引号键输出反引号”开关，不影响组合键。
- 增加带版本、图标和作者信息的“关于”窗口。

### 修改

- 托盘菜单统一使用中文，输入法内部模式统一显示为“（中文）”或“（英文）”。
- 开机自启动改用当前用户 Startup 文件夹中的快捷方式；移动程序后可从新位置重新勾选。
- 前台窗口变化后先等待 Windows 恢复输入法，实际状态不符时才补偿切换。
- 当前窗口用户规则写入 `config.ini`，自动学习状态写入 `state.ini`。
- 左键和右键单击托盘图标都会打开菜单。

### 修复

- 打开折叠托盘时不再把当前窗口误判为 Explorer。
- 不再重复创建名称不同但实际状态相同的输入法配置。
- 自动删除已经不可用的输入法状态、相关规则和学习记录，并修正失效的全局默认值。
- 当前状态已经符合目标时不再重复发送切换消息。
- 修复启动通知名称过长的问题。

## 0.1.2 — 2026-09-16

### 新增

- 增加 Windows 7 Aero 风格程序图标。
- ICO 包含 16、20、24、32、40、48、64、128 和 256 px 尺寸。

### 修改

- 编译版和源码运行模式使用同一程序图标。
- 调整小尺寸图标边界和锐度，改善托盘显示。

## 0.1.1 — 2026-09-16

### 新增

- 按窗口记忆 English US、微信输入法及微信输入法内部中英文状态。
- 支持全局默认、自动学习、持久化、应用用户规则和托盘菜单。
- 提供无需另外安装 AutoHotkey 的独立 EXE。

### 修改

- 目标 profile 和内部模式已经匹配时跳过恢复。
- 只写入实际变化的 IME 状态字段。
- 状态文件使用原子写入，并保留损坏恢复副本。

### 修复

- 修复启动时可能出现的 `buffer` 未赋值错误。
- 未处理异常改为写入日志后退出，避免重复弹出错误窗口。
